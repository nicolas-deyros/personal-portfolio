---
import NavList from '../List/NavList.astro'
import navData from '../../data/navData'
---

<ul class="lg:flex lg:gap-3">
	{
		navData.map(item => (
			<NavList
				href={item.path}
				icon={item.icon}
				text={item.text}
				title={item.text}
				data-navLink
			/>
		))
	}
</ul>

<script>
	import '../../js/nav'
</script>
